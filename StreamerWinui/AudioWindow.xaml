<Window
    x:Class="StreamerWinui.AudioWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StreamerWinui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="140"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid>
            <AppBarButton Icon="Setting" Width="48"/>
            <Grid Name="AppTitleBar"/>
        </Grid>

        <Grid Grid.Row="1" Padding="8">
            <StackPanel Spacing="6">
                
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Name="UpdateDevicesListButton" Click="UpdateDevicesListButton_Click">
                        <SymbolIcon Symbol="Refresh"/>
                    </Button>
                    <ComboBox x:Name="devicesComboBox" DropDownOpened="devicesComboBox_DropDownOpened" SelectionChanged="devicesComboBox_SelectionChanged"></ComboBox>
                    <Button>
                        <SymbolIcon Symbol="Add"/>
                    </Button>
                </StackPanel>
                
                <TextBox Name="pathTextBox"></TextBox>
                <Button Name="StartButton" Click="Button_Click">Startee</Button>
                
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Vertical" Name="StackPanel" Spacing="3" Padding="6"/>
    </Grid>
</Window>
